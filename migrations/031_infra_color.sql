-- =====================================================================
-- Migration 031 — เพิ่มสีของสายรถไฟฟ้า/โครงการ (แยกสีบนแผนที่)
--
-- OSM route relation มี tag "colour" (เช่น #009639) = สีสายจริง
-- load_infra จะอ่านมาเก็บที่คอลัมน์นี้ แล้วแผนที่ระบายสีตามสาย
-- สถานี (จุด) ที่ไม่มีสีของตัวเอง จะยืมสีของ "สายที่ใกล้ที่สุด" อัตโนมัติ
-- =====================================================================

alter table infra_projects add column if not exists line_color text;
